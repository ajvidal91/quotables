<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="style.css">
<link rel="stylesheet" href="font-awesome.min.css">
<link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">
  <meta charset="UTF-8">
  <title>MY APP</title>
</head>
<body>
  <h1>Meaningful Hip-Hop Quotables</h1>

  <form action="/quotable" method="POST">
    <input type="text" placeholder="rapper" name="name">
    <input type="text" name="quote" placeholder="quotable">
    <button type="submit">BARS</button>
  </form>

  <ul class="quotables">
  <% for(var i=0; i<quotable.length; i++) {%>
    <li class="quotable">
      <span><%= quotable[i].name %></span>
      <span><%= quotable[i].quote %></span>
      <span><%= quotable[i].thumbUp %></span>
      <span><%= quotable[i].thumbDown %></span>
      <span><i class="fa fa-thumbs-up" aria-hidden="true"></i></span>
      <span><i class="fa fa-thumbs-down" aria-hidden="true"></i></span>
      <span><i class="fa fa-trash" aria-hidden="true"></i></span>
    </li>
  <% } %>
  </ul>


<script src="main.js"></script>
</body>
</html>
